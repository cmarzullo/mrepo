### Configuration file for mrepo

### The [main] section allows to override mrepo's default settings
### The mrepo-example.conf gives an overview of all the possible settings
[main]
srcdir = <%= node[:mrepo][:config][:srcdir] %>
wwwdir =  <%= node[:mrepo][:config][:wwwdir] %>
confdir = <%= node[:mrepo][:config][:confdir] %>
arch = <%= node[:mrepo][:config][:arch] %>

mailto = <%= node[:mrepo][:config][:mailto] %>
smtp-server = <%= node[:mrepo][:config][:smtpserver] %>

#rhnlogin = username:password

### Any other section is considered a definition for a distribution
### You can put distribution sections in /etc/mrepo.conf.d/
### Examples can be found in the documentation at:
### /usr/share/doc/mrepo-0.8.7/dists/.
